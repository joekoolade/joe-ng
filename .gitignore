# Build artifacts (regenerated by scripts/build.sh from the seed JDK).
out/
kernel8.img
tmp/
sdcard/

# Firmware download archives (extract into ramfs/lib/firmware/; do not commit).
fw.tar.gz
*.tar.gz
