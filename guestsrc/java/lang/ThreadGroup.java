package java.lang;

/**
 * Minimal stub: joe-ng has no thread groups. It exists only so Thread's 5-arg constructor and
 * {@link Thread#getThreadGroup()} have a type to name; the group itself is never used (always null here).
 */
public class ThreadGroup
{
}
